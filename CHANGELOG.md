# Changelog

## [1.0.0] - بازساختاردهی به ساختار ماژولار

### تغییرات
- کد از یک فایل تک (`bot.py`) به ساختار ماژولار (`handlers/`, `core/`, `tg/`, `utils/`) تقسیم شد.
- انتخاب خودکار کیفیت بعد از ۳۰ ثانیه عدم پاسخ.
- نمایش پیشرفت زنده (دانلود و آپلود) داخل پیام تلگرام.
- اتصال تامبنیل ویدیو به فایل ارسالی.
- بهینه‌سازی سرعت دانلود (format selector تک‌مرحله‌ای، aria2c اختیاری، دانلود قطعه‌ای موازی).
- پشتیبانی از کوکی برای عبور از محدودیت‌های یوتیوب.
- استفاده از `.env` برای مقادیر حساس به‌جای هاردکد در کد.
